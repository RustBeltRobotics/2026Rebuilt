TODO:
- Verify with Dillan that MOI isn't changed with recent robot changes (if so, update PathPlanner constants)
- Test shooter full sequence with runIntakeRollerAlternating integrated for ball agitation
    - this command needs to be run with the intake arm down/extended and NOT folded up into the robot
- Test / update CTRE Swerve constants kSlipCurrent (this is how stator current is set)
    - https://docs.advantagekit.org/getting-started/template-projects/talonfx-swerve-template/#slip-current-measurement
    - https://v6.docs.ctr-electronics.com/en/stable/docs/hardware-reference/talonfx/improving-performance-with-current-limits.html#preventing-wheel-slip
- Put robot on blocks and determine kCoupleRatio
    - Rotate the azimuth (steer motor) 3 full rotations using Tuner X or a manual command
    - Observe the drive motor encoder — it will report some rotation even though the wheel didn't move
    - kCoupleRatio = driveMotorRotations / azimuthRotations
    => Just run the RotateAzimuthCommand using a() on operator controller and look at the console output in RioLog
- Test prepVariableDistanceShot command to auto-set shooter RPM based on robot pose distance from hub
- Test Align To Target drive with AprilTags on hub
    - PID turn to hub is working, might need some tuning
- Update robot to camera pose translation constants for new camera locations
- Test intake arm extending in/out to hard stop commands to ensure current based stall detection still works
    - Graph the intake arm current if it does not, and adjust threshold/debounce time accordingly
- Create/measure distance (or set field locations) -> shooter RPM mappings for shots
- Document controller button mappings for driver + operator

- Run robot at full speed forward in a straight line with AdvantageScope open to get measured max velocity in M/S
    - TODO: we don't really have enough space in the lab to do this :(
        - it's definitely above 5.0 m/s though
    - Plot DriveState/Speeds/vxMetersPerSecond
    - Update MAX_VELOCITY_METERS_PER_SECOND and config in PathPlanner GUI to this value
        - https://pathplanner.dev/robot-config.html#-ntfiad_50

--------
Implemented, needs testing:
- Implement ramp detection to set flag for ramp encountered (use tilt from gyro)
    - If ramp was encountered and tilt goes back to flat, set a separate flag to indicate vision odometry should be boosted (or just use resetPose)
        - After that is done, clear all flags
    - Done => needs to be tested to verify
    - This may not be needed if normal vision measurement pose estimates are good enough

- Implement and test robot simulation using CTRE swerve and photonvision support